# hbctool's runtime dependencies (the package only uses the Python standard library).
# This file is kept for `pip install -r requirements.txt` workflows; install the
# package itself with `pip install -e .` for development.
